---
title: "S3 第三章：估计与置信区间"
---

## 从数据到决策：理解样本能告诉我们什么

在第一章中，我们学习了如何从总体中收集有代表性的样本。我们使用适当的抽样方法，从 HelloTea 的 3,000 名客户中精心挑选了 200 名学生。现在我们面临下一个关键问题：

**我们用这些数据做什么？**

完成调查后，我们计算出样本均值满意度得分：$\bar{X} = 4.2$（0 到 5 分制）。这是我们对所有 3,000 名学生真实平均满意度 $\mu$ 的最佳估计。但这个估计有多可靠？

:::note[核心挑战]
**我们的观察：** 200 名学生的样本均值 $\bar{X} = 4.2$

**我们的问题：** 这与所有 3,000 名学生的真实均值 $\mu$ 有多接近？

**根本问题：** 我们永远无法精确知道 $\mu$（除非调查所有人），但我们可以量化估计的可靠程度。
:::

要严格回答这些问题，我们需要一个数学框架。本章发展**统计估计**的理论，并展示如何使用**置信区间**来量化不确定性。

:::tip[本章学习目标]
在本章结束时，你应该能够：

1. 在实际数据问题中区分参数、估计量和估计值。
2. 使用偏差和标准误差判断估计量的质量。
3. 构建并解释总体均值的置信区间。
4. 解释公式背后的假设及其可能失效的情况。
:::

:::tip[本章路线图]
本章的组织方式：

1. **第 1 节（估计量）：** 我们试图估计什么，什么算作好的估计规则。
2. **第 2 节（偏差）：** 估计量是否系统性地偏高或偏低。
3. **第 3 节（标准误差）：** 估计值在不同样本之间的波动程度。
4. **第 4 节（置信区间）：** 如何将一个估计值转化为真实值的合理范围。
5. **第 5 节（总结与反思）：** 需要记住什么，需要质疑什么假设。
:::

### 关键假设

在继续之前，我们必须明确说明方法所依赖的假设。

:::note[基本假设]
**1.（正态性）：** 每个学生的满意度得分来自正态分布：
$$X_i \sim N(\mu, \sigma^2) \text{ for } i = 1, 2, \ldots, 200$$

**2.（独立性）：** 每个学生的评分与其他所有学生独立。

**3.（随机抽样）：** 我们的样本是使用适当的随机抽样方法选择的（如第一章所学）。

**注意：** 这些是*强*假设。我们将在本章末尾重新审视它们的现实性。
:::

## 1. 估计量：起点

:::tip[学习目标]
- 在上下文中识别参数并选择自然的估计量。
- 区分估计量（$\hat{\theta}$）和观测估计值（$\hat{\theta}_{obs}$）。
- 解释为什么估计量必须用数学方法评估，而不仅仅是凭直觉。
:::

### 自然的问题

我们用样本均值 $\bar{X} = 4.2$ 来估计总体均值 $\mu$。这看起来很自然——毕竟，样本的平均值应该能告诉我们总体平均值的某些信息。但我们怎么知道这是一个*好的*估计？

:::note[根本问题]
既然我们观察到 $\bar{X} = 4.2$，我们如何判断这个估计的质量？

"好的估计"在数学上到底意味着什么？
:::

要回答这个问题，我们需要正式定义"估计量"的含义。

:::note[定义之前的真实场景]
一个外卖平台想知道本月所有订单的**平均配送时间**。它只审计了 500 个订单。

- 未知目标是所有订单的真实月平均配送时间。
- 实际规则是使用 500 个审计订单的样本平均值。
- 业务决策（人员配置和补偿政策）取决于这个规则是否可信。

这正是估计量的思想：使用基于样本数据的规则来推断未知的总体量。
:::

**定义：估计量**

**估计量**是一个统计量（样本数据的函数），用于估计未知的总体参数。

**点估计量**产生单个数值作为估计值。

**符号：** 我们通常用 $\hat{\theta}$ 表示参数 $\theta$ 的估计量。

**示例：HelloTea 估计量**

在我们的满意度调查中：
- **总体参数：** $\mu$ = 所有 3,000 名学生的真实平均满意度（未知）
- **估计量：** $\bar{X}$ = 样本均值 = $\frac{1}{n}\sum_{i=1}^{n} X_i$
- **估计值：** $\bar{x} = 4.2$（我们观察到的具体值）

注意：$\bar{X}$ 是一个随机变量（估计量），而 $\bar{x} = 4.2$ 是我们计算出的具体数字。

### 如何判断估计量？

现在我们有了 $\mu$ 的估计量 $\bar{X}$，我们面临两个关键问题：

:::note[关于估计量的两个关键问题]
**问题 1（系统误差）：** 我们的估计量是否系统性地错误？它是否倾向于高估或低估真实参数？

$\Rightarrow$ 这引出了**偏差**的概念（第 2 节）

**问题 2（变异性）：** 我们的估计量在不同样本之间变化有多大？如果我们用不同的 200 名学生随机样本重复调查，$\bar{X}$ 会有多不同？

$\Rightarrow$ 这引出了**标准误差**的概念（第 3 节）
:::

## 2. 偏差：我们是否系统性地错误？

:::tip[学习目标]
- 计算并将偏差解释为长期系统误差。
- 使用期望规则证明估计量是否无偏。
- 解释为什么 $n-1$ 修正在样本方差中出现。
:::

### 定义偏差

第一个问题是我们的估计量是否有系统性偏差——它是否倾向于偏高或偏低？

:::note[偏差之前的真实场景]
一个可穿戴设备估计每日燃烧的卡路里。假设该算法在许多用户中始终比实验室测量低约 80 千卡。

- 每天的个别误差上下波动。
- 但在重复用户和重复天数中，**平均误差**是负的。

这是一个偏差问题：方法系统性地偏离中心，而不仅仅是有噪声。
:::

**定义：偏差**

估计量 $\hat{\theta}$ 对参数 $\theta$ 的**偏差**定义为：
$$\text{Bias}(\hat{\theta}) = E[\hat{\theta}] - \theta$$

如果 $E[\hat{\theta}] = \theta$，即 $\text{Bias}(\hat{\theta}) = 0$，则估计量是**无偏的**。

**解释：** 无偏估计量在"平均意义上是正确的"——如果我们无限次重复抽样过程，所有估计值的平均值将等于真实参数。

### 样本均值是否无偏？

让我们检查 $\bar{X}$ 是否是 $\mu$ 的无偏估计量。

**问题：** $E[\bar{X}] = \mu$ 吗？

要回答这个问题，我们需要计算 $E[\bar{X}]$。线性规则在*随机变量的组合*讲义中已经介绍过，所以我们这里专注于它们在估计中的应用。

现在我们可以检查 $\bar{X}$ 是否无偏：

**示例：样本均值是无偏的**

**计算：**

$$
\begin{aligned}
E[\bar{X}] &= E\left[\frac{1}{200}\sum_{i=1}^{200} X_i\right] = \frac{1}{200} E\left[\sum_{i=1}^{200} X_i\right] = \frac{1}{200} \sum_{i=1}^{200} E[X_i] = \frac{1}{200} \sum_{i=1}^{200} \mu  = \mu
\end{aligned}
$$

**结论：** $E[\bar{X}] = \mu$，所以样本均值是总体均值的**无偏估计量**！

**解释：** 平均而言（在多次重复抽样中），$\bar{X}$ 等于真实均值 $\mu$。我们的具体观察值 $\bar{x} = 4.2$ 可能高于或低于 $\mu$，但方法本身没有系统性偏差。

### 示例：样本方差与偏差的发现

并非所有自然的估计量都是无偏的。让我们检验样本方差。

**示例：有偏的样本方差**

假设我们想估计总体方差 $\sigma^2$。自然的估计量可能是：
$$\text{自然估计量：} \frac{1}{n}\sum_{i=1}^{n}(X_i - \bar{X})^2$$

这只是与样本均值的平均平方偏差。它是否无偏？

**让我们计算**（为简单起见，我们展示关键思路）：

可以证明（请参阅挑战练习了解详情）：
$$E\left[\frac{1}{n}\sum_{i=1}^{n}(X_i - \bar{X})^2\right] = \frac{n-1}{n}\sigma^2$$

**发现：** 这不等于 $\sigma^2$！偏差为：
$$\text{Bias} = \frac{n-1}{n}\sigma^2 - \sigma^2 = -\frac{1}{n}\sigma^2 < 0$$

自然估计量**系统性地低估**了真实方差。

**为什么？**

当我们使用 $\bar{X}$（从相同数据计算得出）时，我们人为地使偏差变小。样本均值 $\bar{X}$ 使 $\sum(X_i - \bar{X})^2$ 最小化，因此使用它会使方差看起来比实际更小。

### 修正的样本方差

为了修正这个偏差，我们使用修正后的公式：

**定义：无偏样本方差**

**样本方差**（$\sigma^2$ 的无偏估计量）是：
$$S^2 = \frac{1}{n-1}\sum_{i=1}^{n}(X_i - \bar{X})^2$$

这个修正因子 $(n-1)$ 称为**自由度**。

**验证：**
$$E[S^2] = E\left[\frac{1}{n-1}\sum_{i=1}^{n}(X_i - \bar{X})^2\right] = \frac{n}{n-1} \cdot \frac{n-1}{n}\sigma^2 = \sigma^2 \checkmark$$

**示例：HelloTea：使用正确的公式**

在我们的 $n = 200$ 名学生的满意度调查中：
- 如果我们计算出 $\sum_{i=1}^{200}(X_i - 4.2)^2 = 288$
- **错误（有偏）：** 方差 $\approx \frac{288}{200} = 1.44$
- **正确（无偏）：** $S^2 = \frac{288}{199} \approx 1.447$

这里的差异很小，但原则很重要：样本方差始终使用 $n-1$！

:::note[练习 1：理解估计中的偏差]
一个质量控制团队测试手机电池寿命。对于来自均值为 $\mu$、方差为 $\sigma^2$ 的总体的随机样本 $X_1, X_2, \ldots, X_n$，团队考虑了两个总体方差的估计量：

估计量 1：$V_1 = \frac{1}{n}\sum_{i=1}^{n}(X_i - \bar{X})^2$

估计量 2：$V_2 = \frac{1}{n-1}\sum_{i=1}^{n}(X_i - \bar{X})^2$

a. 证明 $\bar{X} = \frac{1}{n}(X_1 + X_2 + \cdots + X_n)$ 是总体均值 $\mu$ 的无偏估计量。

b. 已知 $V_2$ 是 $\sigma^2$ 的无偏估计量，求 $V_1$ 作为 $\sigma^2$ 估计量时的偏差。用 $n$ 和 $\sigma^2$ 表示你的答案。

c. 随机抽取了五块电池进行测试。寿命（小时）如下：
$$435 \quad 390 \quad 356 \quad 388 \quad 449$$
计算 $\mu$ 和 $\sigma^2$ 的无偏估计值。

d. 一位研究人员提出了总体均值 $\mu$ 的替代估计量：
$$T = \frac{X_1 + 2X_3 + X_5}{4}$$

   i. 用 $\mu$ 表示计算 $E[T]$。$T$ 是 $\mu$ 的无偏估计量吗？证明你的答案。

   ii. 求 $T$ 作为 $\mu$ 估计量的偏差。

   iii. 解释为什么标准估计量 $\bar{X} = \frac{1}{n}(X_1 + X_2 + \cdots + X_n)$ 优于 $T$。
:::

## 3. 标准误差：衡量精度

:::tip[学习目标]
- 解释为什么仅无偏性不足以进行良好估计。
- 从方差规则推导 $\text{SE}(\bar{X}) = \sigma/\sqrt{n}$。
- 使用标准误差比较不同样本量的精度。
:::

### 超越偏差：理解变异性

我们已经确定 $\bar{X}$ 是无偏的（$E[\bar{X}] = \mu$），这是好消息！但无偏性并不能说明全部问题。考虑这个思想实验：

:::note[思想实验]
想象我们从 HelloTea 的客户群中调查了*不同的*200 名学生随机样本。我们会再次得到完全相同的 $\bar{X} = 4.2$ 吗？

**几乎肯定不会！** 我们会得到不同的值，可能是 $\bar{X} = 4.15$ 或 $\bar{X} = 4.28$。

**关键问题：** $\bar{X}$ 在不同样本之间通常变化有多大？
:::

这种变异性由**标准误差**捕获。

:::note[标准误差之前的真实场景]
一个营销团队在多周内运行相同的在线广告活动。每周他们报告随机客户样本的平均订单价值。

- 估计方法在平均意义上是无偏的。
- 但由于随机抽样，每周的样本均值仍然上下波动。

标准误差量化了估计量的这种周与周之间的波动。
:::

**定义：标准误差**

估计量 $\hat{\theta}$ 的**标准误差**是其抽样分布的标准差：
$$\text{SE}(\hat{\theta}) = \sqrt{\text{Var}(\hat{\theta})}$$

**解释：** 较小的标准误差意味着更精确（变异性更小）的估计量。

在独立抽样下的样本均值：

$$
\text{Var}(\bar{X})=\frac{\sigma^2}{n}
\quad\Rightarrow\quad
\boxed{\text{SE}(\bar{X})=\frac{\sigma}{\sqrt{n}}}.
$$

:::caution[前置知识提醒]
方差组合规则和独立性检查点在*随机变量的组合*讲义中有详细介绍。在本章中，我们将上述标准误差结果作为工作工具，重点放在解释和置信区间的构建上。
:::

### 理解标准误差公式

公式 $\text{SE}(\bar{X}) = \frac{\sigma}{\sqrt{n}}$ 揭示了重要的见解：

:::tip[标准误差公式的见解]
**1. 总体变异性（$\sigma$）的影响：**
- 较大的 $\sigma$（变异性更大的总体）$\Rightarrow$ 较大的标准误差（估计精度较低）
- 较小的 $\sigma$（更同质的总体）$\Rightarrow$ 较小的标准误差（估计精度较高）

**2. 样本量（$n$）的影响：**
- 较大的 $n$（更多数据）$\Rightarrow$ 较小的标准误差（估计精度较高）
- 改进与 $\sqrt{n}$ 成正比，而不是 $n$

**3. $\sqrt{n}$ 关系：**
- 要将标准误差减半，需要 4 倍的数据
- 要将标准误差降至 1/10，需要 100 倍的数据
- 递减回报：样本量翻倍并不会使精度翻倍
:::

### HelloTea 计算

让我们将其应用于满意度调查：

**示例：HelloTea 标准误差**

**场景：** 根据历史数据或试点研究，假设满意度评分的标准差 $\sigma = 1.2$ 分（0-5 分制）。

**我们的样本：** $n = 200$ 名学生，$\bar{X} = 4.2$

**计算标准误差：**
$$\text{SE}(\bar{X}) = \frac{\sigma}{\sqrt{n}} = \frac{1.2}{\sqrt{200}} = \frac{1.2}{14.142} \approx 0.0849$$

**解释：** 样本均值 $\bar{X}$ 通常与真实均值 $\mu$ 相差约 $\pm 0.085$ 分。

如果我们用不同的 200 名学生随机样本多次重复调查，大约 68% 的样本均值会落在 $\mu \pm 0.085$ 范围内。

### 当总体方差未知时

在实践中，我们通常不知道总体标准差 $\sigma$。我们该怎么办？

:::note[估计的标准误差]
当 $\sigma$ 未知时（几乎总是如此），我们使用样本标准差 $S$ 来估计它：

$$\text{估计 SE}(\bar{X}) = \frac{S}{\sqrt{n}}$$

其中 $S = \sqrt{S^2} = \sqrt{\frac{1}{n-1}\sum_{i=1}^{n}(X_i - \bar{X})^2}$

**HelloTea 示例：** 如果我们的样本得出 $S = 1.2$，则：
$$\text{估计 SE}(\bar{X}) = \frac{1.2}{\sqrt{200}} \approx 0.085$$
:::

**注意：** 对于大样本（$n \geq 30$），使用 $\sigma$ 和 $S$ 之间的差异可以忽略不计。

## 4. 置信区间：量化不确定性

:::tip[学习目标]
- 将抽样分布与区间构建联系起来。
- 推导并应用 $\mu$ 的 $100(1-\alpha)\%$ 置信区间。
- 使用样本量、变异性和置信水平解释区间宽度。
:::

### 超越点估计

到目前为止，我们已经学习了：
- $\bar{X} = 4.2$ 是 $\mu$ 的无偏估计（没有系统误差）
- $\text{SE}(\bar{X}) \approx 0.085$ 告诉我们典型的变异性

但当我们向 HelloTea 的经理报告时，说"平均满意度是 4.2，标准误差为 0.085"并不直观。更好的方法是给出 $\mu$ 的**合理值范围**。

:::note[点估计的局限性]
**点估计：** $\bar{X} = 4.2$ 给出一个数字，但是...
- 我们知道这几乎肯定不等于 $\mu$
- 它没有传达我们的不确定性
- 很难用于决策

**更好的方法：** 给出一个范围，如"$\mu$ 可能在 4.0 到 4.4 之间"

这就是**置信区间**提供的！
:::

:::note[置信区间之前的真实场景]
一家医院根据患者记录样本报告平均急诊等待时间。

- 报告单个值（例如 47 分钟）可能误导性地精确。
- 运营经理需要一个合理的范围来安全地规划人员配置。

置信区间为真实平均等待时间提供了一个有原则的不确定性带。
:::

### 抽样分布的作用

要构建有意义的区间，我们需要理解 $\bar{X}$ 的*分布*——不仅仅是它的均值和方差，而是它的整个概率分布。这就是我们的正态性假设变得至关重要的地方！

:::note[为什么我们需要抽样分布]
**问题：** 我们如何知道 $\bar{X}$ 通常偏离 $\mu$ 多远？

**答案：** 我需要知道 $\bar{X}$ 本身的概率分布！

这称为 $\bar{X}$ 的**抽样分布**——描述 $\bar{X}$ 在不同可能样本中如何变化的分布。
:::

### 样本均值的分布

对于置信区间，我们使用关键结果：

$$
\bar{X} \sim N\left(\mu, \frac{\sigma^2}{n}\right)
$$

在独立正态抽样下。

:::caution[与前置讲义的联系]
为什么正态变量的线性组合是正态的，这在*随机变量的组合*讲义中有详细介绍。在这里，我们专注于如何使用这个结果来构建和解释置信区间。
:::

### 构建置信区间

现在我们可以通过标准化 $\bar{X}$ 来构建置信区间：

**示例：推导 95% 置信区间**

**从已知开始：**
$$Z = \frac{\bar{X} - \mu}{\sigma/\sqrt{n}} \sim N(0,1)$$

**使用 95% 概率：**
$$P(-1.96 < Z < 1.96) = 0.95$$

**代入 $Z$ 的公式：**
$$P\left(-1.96 < \frac{\bar{X} - \mu}{\sigma/\sqrt{n}} < 1.96\right) = 0.95$$

**所有部分乘以 $\sigma/\sqrt{n}$：**
$$P\left(-1.96 \cdot \frac{\sigma}{\sqrt{n}} < \bar{X} - \mu < 1.96 \cdot \frac{\sigma}{\sqrt{n}}\right) = 0.95$$

**重新排列以将 $\mu$ 置于中间：**
$$P\left(\bar{X} - 1.96 \cdot \frac{\sigma}{\sqrt{n}} < \mu < \bar{X} + 1.96 \cdot \frac{\sigma}{\sqrt{n}}\right) = 0.95$$

**这可以简洁地写成：**
$$P\left(\bar{X} - 1.96 \cdot \text{SE}(\bar{X}) < \mu < \bar{X} + 1.96 \cdot \text{SE}(\bar{X})\right) = 0.95$$

或者更简单：$\mu \in \left[\bar{X} \pm 1.96 \cdot \text{SE}(\bar{X})\right]$，概率为 0.95

### 置信区间公式

**定义：均值的置信区间**

总体均值 $\mu$ 的 **$100(1-\alpha)\%$ 置信区间**为：

$$\boxed{\bar{X} \pm z^* \times \text{SE}(\bar{X}) = \bar{X} \pm z^* \times \frac{\sigma}{\sqrt{n}}}$$

其中 $z^*$ 的选择使得对于 $Z \sim N(0,1)$，$P(-z^* < Z < z^*) = 1 - \alpha$。

**常用置信水平：**

| 置信水平 | $\alpha$ | $z^*$ 值 |
|:---:|:---:|:---:|
| 90% | 0.10 | 1.645 |
| 95% | 0.05 | 1.96 |
| 99% | 0.01 | 2.576 |

**当 $\sigma$ 未知时：** 用样本标准差 $S$ 替代 $\sigma$：
$$\bar{X} \pm z^* \times \frac{S}{\sqrt{n}}$$

### HelloTea：计算置信区间

让我们为满意度调查整合所有内容：

**示例：HelloTea 95% 置信区间**

**给定数据：**
- 样本量：$n = 200$ 名学生
- 样本均值：$\bar{X} = 4.2$ 分
- 样本标准差：$S = 1.2$ 分
- 置信水平：95%（所以 $z^* = 1.96$）

**第 1 步：计算标准误差**
$$\text{SE}(\bar{X}) = \frac{S}{\sqrt{n}} = \frac{1.2}{\sqrt{200}} = \frac{1.2}{14.142} = 0.0849 \approx 0.085$$

**第 2 步：计算误差范围**
$$\text{误差范围} = z^* \times \text{SE}(\bar{X}) = 1.96 \times 0.085 = 0.1666 \approx 0.167$$

**第 3 步：构建区间**

$$
\begin{aligned}
\text{95\% CI} &= \bar{X} \pm \text{误差范围}\\
&= 4.2 \pm 0.167\\
&= [4.033, 4.367]
\end{aligned}
$$

**报告：** "我们有 95% 的信心，所有 3,000 名学生的真实平均满意度得分在 4.03 到 4.37 分之间。"

### 影响置信区间宽度的因素

置信区间的宽度告诉我们估计的精度。较窄的区间意味着我们更精确地确定了 $\mu$。

:::tip[什么影响置信区间宽度？]
置信区间的宽度为 $2 \times z^* \times \frac{\sigma}{\sqrt{n}}$

**因素 1：样本量（$n$）**
- 较大的 $n$ $\Rightarrow$ 较窄的置信区间（更精确）
- 宽度以 $1/\sqrt{n}$ 的速度减小
- 要将宽度减半，需要 4 倍的样本量

**因素 2：总体变异性（$\sigma$）**
- 较大的 $\sigma$（变异性更大的总体）$\Rightarrow$ 较宽的置信区间（精度较低）
- 无法控制——这是总体的属性
- 更同质的总体给出更精确的估计

**因素 3：置信水平**
- 较高的置信水平 $\Rightarrow$ 较宽的置信区间
- 90% 置信区间：$z^* = 1.645$（较窄）；95% 置信区间：$z^* = 1.96$（适中）；99% 置信区间：$z^* = 2.576$（较宽）
- 权衡：更高的置信度需要更宽的网
:::

**示例：HelloTea：比较不同的置信水平**

使用 $\bar{X} = 4.2$，$S = 1.2$，$n = 200$，所以 $\text{SE} = 0.085$：

| 置信水平 | 计算 | 区间 |
|:---:|:---:|:---:|
| 90% | $4.2 \pm 1.645(0.085)$ | [4.06, 4.34] |
| 95% | $4.2 \pm 1.96(0.085)$ | [4.03, 4.37] |
| 99% | $4.2 \pm 2.576(0.085)$ | [3.98, 4.42] |

**观察：** 更高的置信度意味着撒更宽的网。我们更有信心区间包含 $\mu$，但区间告诉我们 $\mu$ 的位置不那么精确。

## 5. 总结与批判性反思

:::tip[学习目标]
- 整合从点估计到区间估计的完整估计工作流程。
- 批判性地评估基于正态推断背后的假设。
- 为中央极限定理作为鲁棒性工具做准备。
:::

### 我们学到了什么

本章发展了统计估计的数学框架：

:::note[关键概念总结]
1. **估计量：** 用于估计未知总体参数的统计量
   - 示例：$\bar{X}$ 估计 $\mu$

2. **偏差：** 衡量系统误差
   - 公式：$\text{Bias}(\hat{\theta}) = E[\hat{\theta}] - \theta$
   - 无偏：$E[\hat{\theta}] = \theta$
   - 结果：$\bar{X}$ 对 $\mu$ 是无偏的

3. **标准误差：** 衡量精度/变异性
   - 公式：$\text{SE}(\hat{\theta}) = \sqrt{\text{Var}(\hat{\theta})}$
   - 对于 $\bar{X}$：$\text{SE}(\bar{X}) = \sigma/\sqrt{n}$
   - 较小的标准误差 = 更精确的估计

4. **置信区间：** 量化不确定性
   - 公式：$\bar{X} \pm z^* \times \text{SE}(\bar{X})$
   - 解释：方法以指定概率捕获 $\mu$
   - 报告不确定性的实用工具
:::

### 关键公式总结

| 概念 | 公式 | HelloTea 值 |
|:---|:---:|:---:|
| 样本均值 | $\bar{X} = \frac{1}{n}\sum_{i=1}^{n}X_i$ | 4.2 |
| $\bar{X}$ 的期望 | $E[\bar{X}] = \mu$ | $\mu$（无偏） |
| $\bar{X}$ 的方差 | $\text{Var}(\bar{X}) = \frac{\sigma^2}{n}$ | $\frac{1.44}{200}$ |
| 标准误差 | $\text{SE}(\bar{X}) = \frac{\sigma}{\sqrt{n}}$ | 0.085 |
| 95% 置信区间 | $\bar{X} \pm 1.96 \times \text{SE}(\bar{X})$ | [4.03, 4.37] |
| 样本方差 | $S^2 = \frac{1}{n-1}\sum_{i=1}^{n}(X_i-\bar{X})^2$ | 1.44 |

### 假设的批判性检验

在本章中，我们依赖了几个关键假设。让我们批判性地评估它们：

:::caution[假设 1：正态性]
**我们假设：** $X_i \sim N(\mu, \sigma^2)$——满意度得分服从正态分布

**现实检验：**
- 满意度评分是**离散的**（1, 2, 3, 4, 5），不是连续的
- 正态分布是连续的，范围从 $-\infty$ 到 $+\infty$
- 可能有**天花板效应**（许多学生给 5/5）或**地板效应**
- 分布可能是**偏态的**（不对称）
- 评分行为可能根本不遵循钟形曲线
:::

:::caution[假设 2：独立性]
**我们假设：** 每个学生的评分与其他学生独立

**现实检验：**
- 学生可能互相影响（"我朋友说很棒！"）
- 如果我们抽样了一起访问的学生，评分可能相关
- 社交媒体帖子可能导致意见聚集

**好消息：** 随机抽样有助于确保独立性。如果我们真正从总体中随机选择学生，这个假设是合理的。
:::

### 大问题：如果正态性失效怎么办？

:::note[一个根本问题]
**我们推导出：** 如果 $X_i \sim N(\mu, \sigma^2)$，则 $\bar{X} \sim N(\mu, \sigma^2/n)$

**这对以下方面至关重要：** 使用标准正态分布构建置信区间

**但如果 $X_i$ 不是正态的：** 我们的整个方法会崩溃吗？
:::

### 预览：中央极限定理来救援

我们担忧的答案来自统计学中最强大的结果之一：

:::tip[预览：中央极限定理]
**惊人的结果：**

即使总体不是正态分布的，随着样本量 $n$ 的增大，样本均值 $\bar{X}$ 也近似服从正态分布！

$$\text{对于大的 } n: \quad \bar{X} \approx N\left(\mu, \frac{\sigma^2}{n}\right) \quad \text{无论总体分布的形状如何}$$

**为什么它很了不起：**
- 总体可以是偏态的、离散的、有界的、双峰的——都没关系！
- *平均值*通过平均的魔力趋向正态性
- 这就是为什么正态分布在统计学中如此核心
:::

:::note[练习 2：WST03/01/May16/7]

a. 从果园随机抽取 8 个苹果，测量每个苹果的重量（克）。结果如下。
$$143 \quad 131 \quad 165 \quad 122 \quad 137 \quad 155 \quad 148 \quad 151$$
计算苹果重量的均值和方差的无偏估计值。(4)

b. 一个总体的均值 $\mu$ 和方差 $\sigma^2$ 均未知。从该总体中抽取一个由 $X_1, X_2, X_3, \ldots, X_8$ 表示的随机样本。解释为什么 $\sum_{i=1}^8 (X_i - \mu)^2$ 不是一个统计量。(1)

c. 已知 $E(S^2) = \sigma^2$，其中 $S^2$ 是 $\sigma^2$ 的无偏估计量，且统计量
$$Y = \frac{1}{8} \left( \sum_{i=1}^8 X_i^2 - 8\overline{X}^2 \right)$$
用 $\sigma^2$ 表示求 $E(Y)$。(2)

d. 因此求 $Y$ 作为 $\sigma^2$ 估计量时的偏差，用 $\sigma^2$ 表示。(2)
:::

:::note[练习 3：WST03/01/May17/6]

a. 一家公司生产某种类型的杯子。这些杯子的质量服从均值为 $\mu$、标准差为 1.2 克的正态分布。随机抽取 5 个杯子，测量每个杯子的质量（克）。结果如下。
$$229.1 \quad 229.6 \quad 230.9 \quad 231.2 \quad 231.7$$
求 $\mu$ 的 95% 置信区间，给出精确到小数点后 1 位的界限。(4)

b. Sonia 计划抽取 20 个随机样本，每个样本 5 个杯子。每个样本确定一个 $\mu$ 的 95% 置信区间。求超过 3 个区间不包含 $\mu$ 的概率。(3)
:::

:::note[练习 4：6691/01/June13/7]

a. 羊羔在 Mill Farm 的一个棚子里出生。随机抽取 8 只新生羊羔的出生体重 $x$（千克）如下。
$$4.12 \quad 5.12 \quad 4.84 \quad 4.65 \quad 3.55 \quad 3.65 \quad 3.96 \quad 3.40$$
计算 Mill Farm 出生羊羔体重的均值和方差的无偏估计值。(3)

b. 再随机抽取 32 只羊羔，该样本的体重均值和方差的无偏估计值分别为 4.55 和 0.25。将 40 只羊羔的合并样本视为单个样本，估计均值的标准误差。(7)

c. Mill Farm 的业主研究了羊羔品种，发现出生体重的总体服从标准差为 0.67 千克的正态分布。使用合并样本均值计算该品种羊羔平均出生体重的 95% 置信区间。(3)
:::

:::note[练习 5：6691/01/May14/6]

a. 从均值为 $\mu$ 的总体中抽取随机样本 $X_1, X_2, \ldots, X_n$。证明
$$\overline{X} = \frac{1}{n} (X_1 + X_2 + \ldots + X_n)$$
是总体均值 $\mu$ 的无偏估计量。(1)

b. 一家公司生产小罐咖啡。随机抽取五罐咖啡称重。重量（克）如下：
$$197 \quad 203 \quad 205 \quad 201 \quad 195$$
计算公司生产的咖啡罐重量的总体均值和方差的无偏估计值。(4)

c. 已知重量服从标准差为 4.8 克的正态分布。经理将抽取第二个随机样本。他希望确保总体均值的估计值在其真实值 1.25 克范围内的概率至少为 95%。求所需的最小样本量。(4)
:::

:::note[练习 6：6691/01/May16/7]

a. 一家餐厅声称其汉堡含有 20% 的脂肪。Paul 声称该餐厅汉堡的平均脂肪含量低于 20%。Paul 从该餐厅随机抽取了 50 个汉堡，发现平均脂肪含量为 19.5%，标准差为 1.5%。你可以假设汉堡的脂肪含量服从正态分布。求该餐厅汉堡平均脂肪含量的 90% 置信区间。(4)

b. 说明并给出理由，Paul 应该建议餐厅对其声称的汉堡脂肪含量采取什么行动。(2)

c. 餐厅将汉堡的平均脂肪含量改为 $\mu\%$，并将标准差调整为 2%。Paul 从这批新汉堡中抽取了大小为 $n$ 的样本。他用样本均值 $\overline{X}$ 作为 $\mu$ 的估计量。求 $n$ 的最小值，使得
$$P(|\overline{X} - \mu| < 0.5) \geq 0.9.$$ (4)
:::

:::note[练习 7：WST03/Jan2022/1]

a. 从 Priya 的商店随机选取 10 个西瓜，记录每个西瓜的重量 $x$（千克）。结果汇总如下：
$$\sum x = 114.2 \quad \sum x^2 = 1310.464$$
计算 Priya 商店中西瓜重量的均值和方差的无偏估计值。(3)

b. Priya 研究了她商店中西瓜的重量，发现这些西瓜的重量服从标准差为 0.8 千克的正态分布。计算 Priya 商店中西瓜平均重量的 95% 置信区间。将置信区间的界限精确到小数点后 2 位。(4)

c. Priya 声称 (b) 部分的置信区间表明她商店中几乎所有西瓜的重量都超过 10.5 千克。使用 (b) 部分的答案估计她商店中重量低于 10.5 千克的西瓜的最小比例。(3)
:::
