---
title: "S3 第二章：随机变量的组合"
---

:::tip[核心动机]
在实际决策中，我们几乎从不单独使用一个原始测量值。我们使用总和、差值、加权平均和综合得分：
- 质量控制中多个产品的总重量，
- 早晚血压的差值，
- 多个部分的平均测试分数，
- 多阶段物流的总配送时间。

所有这些都需要 **随机变量的组合（Combinations of Random Variables）**。如果学生混淆了期望规则、方差规则或独立性条件，后续的所有推断都会变得脆弱。
:::

:::tip[学习目标]
在本讲义结束时，学生应能够：
1. 使用正态分布记号并在情境中解释参数。
2. 求独立正态变量之和与线性组合的分布。
3. 解释为什么 $E(X+Y)=E(X)+E(Y)$ 总是成立。
4. 清楚地区分 $\text{Var}(X+Y)$ 和 $\text{Var}(X)+\text{Var}(Y)$。
5. 在简化方差计算 **之前** 检查独立性。
6. 解决涉及 $aX+bY$、$\bar{X}$ 和总和的考试题。
:::

:::tip[路线图]
如何使用本讲义：
1. 第 1 节：正态分布基础与直觉。
2. 第 2 节：期望与线性组合。
3. 第 3 节：方差与独立性检查点。
4. 第 4 节：正态分布的组合。
5. 第 5 节：真题风格的课堂练习。
6. 第 6 节：从上次作业转移过来的三道练习题。
:::

## 定义与定理

**定义：正态分布（Normal Distribution）**
随机变量 $X$ 服从均值为 $\mu$、方差为 $\sigma^2$ 的正态分布，记作
$$X \sim N(\mu,\sigma^2).$$
- $\mu$：中心/位置（期望值），
- $\sigma$：离散程度/典型偏差，
- $\sigma^2$：方差。

**定理：期望的线性性（Linearity of Expectation）**
对任意随机变量 $X_1,\ldots,X_n$ 和常数 $a_1,\ldots,a_n,b$，
$$E\!\left(\sum_{i=1}^{n} a_iX_i + b\right)=\sum_{i=1}^{n}a_iE(X_i)+b.$$
**不需要独立性假设。**

**定理：独立条件下的方差规则（Variance Rules Under Independence）**
如果 $X$ 和 $Y$ 独立，则
$$\text{Var}(X+Y)=\text{Var}(X)+\text{Var}(Y).$$
对于独立的 $X_1,\ldots,X_n$，
$$\text{Var}\!\left(\sum_{i=1}^{n}a_iX_i\right)=\sum_{i=1}^{n}a_i^2\text{Var}(X_i).$$

**定理：独立正态变量的线性组合（Linear Combination of Independent Normals）**
如果 $X_1,\ldots,X_n$ 独立且
$$X_i\sim N(\mu_i,\sigma_i^2),$$
则对于常数 $a_1,\ldots,a_n$，
$$L=\sum_{i=1}^{n}a_iX_i$$
服从正态分布，且
$$E(L)=\sum_{i=1}^{n}a_i\mu_i,\quad \text{Var}(L)=\sum_{i=1}^{n}a_i^2\sigma_i^2.$$

## 正态分布：模型与直觉

### 学习目标
- 读取并解释 $X \sim N(\mu,\sigma^2)$。
- 将均值和方差与现实情境联系起来。
- 标准化为 $Z$ 分布以进行概率计算。

### 定义与记号

:::tip[现实动机]
许多量受到许多微小叠加因素的影响：
- 产品重量 = 机器设定 + 材料波动 + 水分 + 测量噪声；
- 通勤时间 = 基础行程时间 + 红绿灯延误 + 上车延误 + 随机人群效应；
- 血压读数 = 生理水平 + 暂时性压力 + 仪器噪声。

当许多微小效应叠加时，钟形曲线通常是一个有用的近似。
:::

### 标准化与概率

如果 $X \sim N(\mu,\sigma^2)$，定义
$$Z=\frac{X-\mu}{\sigma}\sim N(0,1).$$

则
$$P(X\le x)=P\!\left(Z\le \frac{x-\mu}{\sigma}\right).$$

## 例题

**例题：电池重量**

假设电池重量 $W \sim N(75,\,3^2)$ 克。
$$P(W>80) = P\!\left(Z>\frac{80-75}{3}\right) = P(Z>1.667).$$
因此问题转化为标准正态分布查表。

:::tip[关键区别]
$$E(X+Y)=E(X)+E(Y) \quad \text{总是成立}$$

$$\text{Var}(X+Y)=\text{Var}(X)+\text{Var}(Y) \quad \text{不一定成立}$$

这是本章最重要的对比之一。
:::

**例题：对冲交易：美团与阿里巴巴的期望收益**

设
- $M$：美团股票的一日收益率 (%)
- $A$：阿里巴巴股票的一日收益率 (%)

一位交易员构建了一个对冲组合：
$$P=M-0.7A$$
（做多美团，做空 0.7 单位阿里巴巴）。

假设
$$E(M)=0.40,\quad E(A)=0.25.$$
则
$$E(P)=E(M)-0.7E(A)=0.40-0.7(0.25)=0.225.$$
所以对冲组合的期望一日收益率为 $0.225\%$。

**例题：对冲组合方差：已知独立性**

继续
$$P=M-0.7A,$$
并假设
$$\text{Var}(M)=2.25,\quad \text{Var}(A)=1.44.$$
如果题目说明 $M$ 和 $A$ 独立，则
$$\text{Var}(P)=\text{Var}(M)+(-0.7)^2\text{Var}(A)=2.25+0.49(1.44)=2.9556.$$
因此
$$\text{SD}(P)=\sqrt{2.9556}\approx 1.72.$$
**要点：** 在方差计算中，系数必须平方。

:::tip[考试级检查清单：在任何方差求和之前]
1. 写出清晰的随机变量表达式。
2. 问：这些项是否独立？
3. 如果给出了独立性，应用方差求和规则并对每个系数平方。
:::

**例题：对冲组合方差：未给出独立性**

如果题目只给出
$$\text{Var}(M)=2.25,\quad \text{Var}(A)=1.44,$$
但没有说明美团和阿里巴巴的收益率是否独立，则对于
$$P=M-0.7A$$
你 **不能** 直接写
$$\text{Var}(P)=2.25+0.49(1.44).$$

如果收益率负相关，则 $P$ 的方差会变小，从而降低对冲组合的风险。

**例题：血压读数的差值**

早晚血压：
$$M\sim N(120,25),\quad E\sim N(115,36),$$
具有独立性。定义 $D=M-E$。

则
$$E(D)=120-115=5,\quad \text{Var}(D)=25+36=61.$$
所以
$$D\sim N(5,61).$$

**例题：加权组合**

假设 $X\sim N(60,4)$，$Y\sim N(45,9)$，独立。
设
$$T=2X-3Y.$$
则
$$E(T)=2(60)-3(45)=-15,$$
$$\text{Var}(T)=2^2(4)+(-3)^2(9)=16+81=97.$$
因此
$$T\sim N(-15,97).$$

### 样本均值作为一种组合

如果 $X_1,\ldots,X_n$ 是独立的 $N(\mu,\sigma^2)$，则
$$\bar{X}=\frac{1}{n}\sum_{i=1}^{n}X_i$$
也服从正态分布，且
$$\bar{X}\sim N\!\left(\mu,\frac{\sigma^2}{n}\right).$$

这个单一结果是后续置信区间工作的核心基础。

## 练习

:::note[练习：课堂练习]
随机变量 $X_1,\ldots,X_{40}$ 独立且 $X_i\sim N(120,25)$。设 $\bar{X}$ 为样本均值。

(a) 求 $\bar{X}$ 的分布。

(b) 求 $P(\bar{X}>122)$。

(c) 用一两句话解释为什么 $\bar{X}$ 的变异性比单个 $X_i$ 小得多。
:::

## 真题

:::note[练习：WST03/01/June20/7]
(a) 一家公司生产板球和网球。板球的重量 $C$ 克服从正态分布
$$C \sim N(160, 1.25^2)$$
随机选取三个板球。求它们的总重量超过 475.8 克的概率。(4)

(b) 网球的重量 $T$ 克服从正态分布
$$T \sim N(60, 2^2)$$
随机选取五个网球和两个板球。求五个网球和两个板球的总重量超过 625 克的概率。(4)

(c) 从网球中随机抽取一个大小为 $n$ 的样本 $T_1, T_2, T_3, \ldots, T_n$。随机变量
$$Y = (n - 1)T_1 - \sum_{i=2}^n T_i$$
已知
$$\text{P}(Y > 40) = 0.0838$$
精确到 4 位小数，求 $n$。(8)
:::

:::note[练习：6691/01/May19/7]
(a) 两个独立的随机样本 $X_1, X_2, X_3, X_4$ 和 $Y_1, Y_2, Y_3, Y_4$ 分别取自均值为 $\mu$、方差为 $\sigma^2$ 的正态总体。求随机变量 $M = 4X_1 - 3X_2 - \overline{Y}$ 的分布。(4)

(b) 由此证明 $P(4X_1 < 3X_2 + \overline{Y} + \sigma) = 0.579$（精确到 3 位有效数字）。(3)

(c) 一个随机样本 $W_1, W_2, W_3, W_4$ 也取自均值为 $\mu$、方差为 $\sigma^2$ 的正态总体。求随机变量 $T = 4W_1 - 3W_2 - \overline{W}$ 的分布。(5)
:::

:::note[练习：WST03/01/Jan21/6]
(a) 一位陶艺家制作两种颜色的装饰砖：红色和黄色。红砖的长度 $R$ 厘米服从均值为 15 厘米、标准差为 1.5 厘米的正态分布。黄砖的长度 $Y$ 厘米服从正态分布 $N(12, 0.8^2)$。随机变量 $R$ 和 $Y$ 独立。随机选取一块红砖和一块黄砖。求黄砖比红砖长的概率。(4)

(b) Taruni 购买了 3 块红砖和 1 块黄砖。求 3 块红砖的总长度小于黄砖长度的 4 倍的概率。(7)

(c) Stefan 定义随机变量 $X = aR + bY$，其中 $a$ 和 $b$ 是常数。他想使用使 $X$ 的均值为 780 且方差最小的 $a$ 和 $b$ 值。求 Stefan 应使用的 $a$ 值和 $b$ 值。(7)
:::
